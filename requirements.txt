torch
gymnasium
pyglet
numpy
matplotlib

# Notes:
# - Install `torch` from https://pytorch.org to pick the correct CUDA-enabled wheel for your system.
# - You can install these with:
#     pip install -r requirements.txt
# - If you need a GPU-enabled torch, follow the PyTorch installation selector.